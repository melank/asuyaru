# AGENTS.md

asuyaru は、休日の前夜に明日の時間割を組む、個人向けの日本語のアプリである。ページはその Mac 上の Golang が出し、ブラウザで開く。ターミナルでローカルホストを起動しているあいだだけ動作する。
エージェントはプロダクトの言葉で書く。

## どこに書くか

README には、陳腐化しない製品の要旨だけを書く。
それが「概要」である。
決定事項は `doc/requirements.md` に書く。
タスクの分け方、雨、保存の形、一日の枠もそこである。
逆にしない。

README にライセンスへの一行を書いてよい。

## 対象

README、コメント、UI の文言、コミットメッセージ、PR 本文は、利用者が読む日本語である。
このファイルの規則は、それらすべてに適用する。

## 禁止: AI 口調

中身のない型で「ちゃんと書いている感」だけを付けない。
次を使わない。

- 予告と総括: 「重要なのは」「本章では」「まとめると」「〜に他ならない」
- 姿勢だけの宣言: 「正面から扱う」「包括的に」「多角的に」
- 空虚な強調: 「不可欠」「核心的」「鍵となる」「非常に」「シームレス」「直感的」
- 空虚な動詞だけで終わる文: 「掘り下げる」「言語化する」「サポートする」
- 英語の空句: empower, unlock, delight, leverage

同じ主張を言い換えて繰り返さない。
比喩でドメイン語を置き換えない。
動詞は、対象に対する操作そのものを書く。
用語の意味は、確認済みのプロダクト要件を実装より優先する。

## 禁止: 現状メモ

次をドキュメントにもコミットメッセージにも書かない。

- 「アプリはまだ無い」「これは概念の置き場である」
- 「いま作るのは」「まだやらない」「未実装」
- 「v1 では」「将来は」
- 検討の経緯、却下した案、いまのフェーズ

## 禁止: 技術の発明

ページの出し方と、このファイルの「実装」に書いたやり方から外れない。別の実現手段を足さない。
README にインストール手順、起動方法、スタック名を書かない。

## 用語

ドメインの語を使う。
予定、TODO、定期、時間割、起床、終わりの時間、雨天中止、雨天決行、できた、できなかった、一言日記、負担、数重視、時間重視。
同じ対象を別の言い方で増やさない。
「ここまで組む」は使わない。

## 実装

この節はコードに適用する。
言語の表記は Golang である。
ツールのコマンド名は `go` のままである。
製品の振る舞いがこの節と食い違うときは、`doc/requirements.md` を採る。

### 配置

モジュールパスは `github.com/melank/asuyaru` である。
エントリは `cmd/asuyaru` だけである。
そこでは、フラグを読み、依存を組み立て、サーバを起動する。
規則の判断は置かない。

アプリケーションのコードは `internal` に置く。

- `internal/plan` に、予定、TODO、定期、時間割の規則を置く。雨、重なり、起床から終わりの時間までの範囲もここである。HTTP と SQLite をここに入れない。
- `internal/store` に、SQLite の読み書きとマイグレーションを置く。
- `internal/server` に、ルーティング、ハンドラ、HTML テンプレートを置く。
- `internal/jev` に、Jev への HTTP 呼び出しを置く。
- `internal/notify` に、macOS の通知センターへの通知を置く。

この五つより細かい層は足さない。
ウェブフレームワーク、別プロセスのフロントエンドのビルド、常駐エージェントは足さない。

### 起動と停止

待ち受けは `127.0.0.1` だけである。
既定は `127.0.0.1:8080` である。
ホストがループバックでなければ、起動しない。
指定したアドレスで待ち受けを開始できなければ、終了する。
別のポートでは待ち受けない。

SQLite のパスはフラグで差し替えられる。
フラグが無いときの既定は、`os.UserConfigDir` の下の `asuyaru/asuyaru.db` である。

起動の順は、フラグ、SQLite を開く、マイグレーション、テンプレートの解析、待ち受け、である。
割り込み（SIGINT、SIGTERM）で `http.Server.Shutdown` を呼び、SQLite を閉じる。
パニックでプロセスを終了させない。

### HTTP

HTTP は標準ライブラリの `net/http` である。
ルーティングは Golang 1.22 以降の `ServeMux`（メソッドとパス）である。
`http.ListenAndServe` は使わない。
`http.Server` にタイムアウトを付ける。

- `ReadHeaderTimeout` は 5 秒。
- `ReadTimeout` は 10 秒。
- Jev を呼ぶ `http.Client` の `Timeout` は 60 秒。
- `WriteTimeout` は 90 秒。Jev の `Timeout` より短くしない。
- `IdleTimeout` は 120 秒。

ハンドラは、入力を読み、`internal/plan` か `internal/store` を呼び、結果を書く。
ハンドラの中に、雨や重なりや推薦の規則を書かない。
リクエストの `context.Context` を、SQLite と Jev へ渡す。
キャンセルされたあとに書き込みを続けない。

状態を変える操作は POST である。
GET で保存しない。
POST には CSRF トークンを付ける。
トークンは Cookie とフォームの両方に置き、一致しない POST は拒む。
Cookie は `HttpOnly` かつ `SameSite=Lax` である。

ハンドラでパニックしたら、ログに書いて 500 を返す。
原因は `log/slog` に書く。
画面には日本語の短い文を出す。
日誌の本文と、Jev の鍵は、ログに出さない。
エラーは `fmt.Errorf("…: %w", err)` で包む。
返ったエラーを捨てない。

### ページ

HTML は `html/template` である。
`text/template` で HTML を出さない。
テンプレートは起動時に一度だけ解析し、`embed.FS` でバイナリに含める。
リクエストのたびにファイルを読み直さない。
識別子とパッケージ名は英語である。
画面の文は日本語である。

### SQLite

アクセスは `database/sql` と `github.com/mattn/go-sqlite3` である。
ドライバはこれ一つである。
プロセスにつき `*sql.DB` は一つである。
`SetMaxOpenConns(1)` にする。
二台の Mac から同じファイルを開くことは、プロセスをまたいだ SQLite のロックで起きる。
アプリ独自の同期は足さない。

DSN では外部キーをオンにし、`_busy_timeout` を 5000 ミリ秒にする。
ジャーナルは SQLite の既定のロールバックジャーナルである。
共有の単位はファイル一つなので、WAL の脇ファイルは作らない。
書き込みのトランザクションは `BEGIN IMMEDIATE` で始める。
クエリに利用者の入力を連結しない。
プレースホルダを使う。

マイグレーションは、番号の付いた SQL ファイルを `embed.FS` から順に適用する。
一つずつトランザクションで適用し、適用済みを同じ SQLite に記録する。
Jev の日次 3 回も同じファイルに残す。
プロセスを起動し直しても、その暦日の回数は戻らない。
回数の日付は、Mac のローカルの暦日である。

### Jev の呼び出し

Jev の URL と鍵は環境変数から読む。
ソースとログに書かない。
`http.DefaultClient` は使わない。
呼び出し先が無いときは、ネットワークへ送らない。
送っていない呼び出しは、日次 3 回に数えない。
送ったあとの失敗と、その再送は、1 回として数える。
利用者の操作なしに、失敗を次のリクエストへ広げない。
計画を 1 回作ること、または 1 回作り直すことが、リクエスト 1 回である。

### 通知

通知は、起動しているプロセスの中から出す。
別プロセスや launchd のジョブは足さない。
その日の最後のブロックの終了時刻をプロセスが見て、macOS の通知センターへ出す。
出す処理は `osascript` の `display notification` にまとめる。
呼び出しは `internal/notify` の一箇所である。
計画を上書きしたら、その終了時刻の通知だけを差し替える。
この監視で Jev は呼ばない。

### 時刻

時間割の日付と時計時刻は、Mac のローカル時刻である。
UTC に変換して、日付や時計時刻をずらさない。
日付は `YYYY-MM-DD`、時計時刻は時と分で保存する。

### テスト

テストは対象のパッケージの隣に置く。
規則は HTTP を通さず `internal/plan` で試す。
ハンドラは `httptest` で試す。
実ブラウザと、利用者の SQLite ファイルは使わない。
SQLite のテストは `t.TempDir` のファイルを使う。
`gofmt`、`go vet`、`go test ./...` を通す。
`*sql.DB` やサーバのように共有状態を持つパッケージは、`go test -race` も通す。

### 依存

直接の依存は `github.com/mattn/go-sqlite3` だけを足す。
それ以外は標準ライブラリで書く。
`go.sum` をリポジトリに含める。
