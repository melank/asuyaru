#!/bin/sh
export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
if ! command -v gitleaks >/dev/null 2>&1; then
	echo "gitleaks が無いのでコミットを止めた" >&2
	exit 1
fi
gitleaks protect --staged --redact --no-banner --verbose
